diff --git a/ESmodules/state/events.js b/ESmodules/state/events.js
--- a/ESmodules/state/events.js
+++ b/ESmodules/state/events.js
@@
 export function sendSBS_Data(item, data, target, startIndex, endIndex) {
     item.currentBatch = data;
     item.batchStartIndex = startIndex;
     item.batchEndIndex = endIndex;
 
     const SBSData_BatchEvent = new CustomEvent('sbs_batch', {
         detail: {
-            status: 'batch'
+            status: 'batch',
+            batch: data,
+            startIndex,
+            endIndex
         }
     });
     
     target.dispatchEvent(SBSData_BatchEvent);
 };
+
+export function sendSBS_Done(target) {
+    const SBSDoneEvent = new CustomEvent('sbs_done', {
+        detail: {
+            status: 'done'
+        }
+    });
+
+    target.dispatchEvent(SBSDoneEvent);
+}
diff --git a/ESmodules/core/SBSoutputManager.js b/ESmodules/core/SBSoutputManager.js
--- a/ESmodules/core/SBSoutputManager.js
+++ b/ESmodules/core/SBSoutputManager.js
@@
 import { state, stateTarget, speedState, SBSconfig } from "../state/state.js";
-import { sendSBS_Data } from "../state/events.js";
+import { sendSBS_Data, sendSBS_Done } from "../state/events.js";
 
 // SBS = Step By Step 
 
-const SBSeventTarget = new EventTarget();
-
 export let currentBatch = []; // I decided to make it so even if batch size is 1, it still gets send as an array 
-export let resumeSBS = null;
+let timerId = null;
 
-export function SBSoutput() {
-    
-    let visibleMax = SBSconfig.currentMaxNum; // this is the max number that is displayed right now, in the middle of the SBS process.
-    let csi = SBSconfig.currentStepIndex;
-    // const addedBatch = []; <-- might not be necessary due to currentBatch 
-    
-    
-    const startSBS = () => {
-        if (!SBSconfig.isRunning) return;
-        
-        const workerResultLen = state.workerListLen;
-        const ms = speedState.intervalMs;
-        const bs = speedState.batchSize;
+function clearSBSTimer() {
+    if (timerId !== null) {
+        clearTimeout(timerId);
+        timerId = null;
+    }
+}
 
-        if (csi >= workerResultLen) {
-            SBSconfig.isRunning = false;
-            SBSconfig.doneRunning = true;
-            return;
-        }
+function finishSBS() {
+    clearSBSTimer();
+    SBSconfig.isRunning = false;
+    SBSconfig.doneRunning = true;
+    sendSBS_Done(stateTarget);
+}
 
-        currentBatch = state.workerResult.slice(csi, csi + bs);
-        // let currentNumber = state.workerResult[csi]; <-- might be the wrong way to do this
-        let currentNumber = currentBatch[currentBatch.length - 1]; // get the last number in the batch
-        
-        if (currentNumber === undefined) {
-            console.error('Index overflow:', csi);
-            SBSconfig.isRunning = false;
-            return;
-        }
+export function pauseSBS() {
+    SBSconfig.isRunning = false;
+    clearSBSTimer();
+}
 
-        SBSconfig.visibleItems.push(...currentBatch);
-        // addedBatch.push(...currentBatch);
+export function resumeSBS() {
+    if (SBSconfig.doneRunning) return;
+    if (state.workerResult.length === 0) return;
 
-        // if (visibleMax === null || currentNumber > visibleMax) { // oh shoot, it only checks if the last number in the batch is 
-        //     visibleMax = currentNumber;                          // greater than the current max, not all numbers in the batch.
-        //     SBSconfig.currentyDisplayedMaxNum = visibleMax;      // I need to check all numbers in the batch to find the maxпј€пј›ВґРґпЅЂпј‰г‚ћ
-        // }
+    clearSBSTimer();
+    SBSconfig.isRunning = true;
+    runSBSTick();
+}
 
-        // const batchMax = Math.max(...currentBatch); <-- Doesn't work with BigInt
-        for (let i = 0; i < currentBatch.length; i++) {
-            if (visibleMax === 0n || currentBatch[i] > visibleMax) {
-                visibleMax = currentBatch[i];
-            }
-        }
+export function startSBS() {
+    clearSBSTimer();
 
-        
-        csi = csi + currentBatch.length;
-        
-        SBSconfig.batchStartIndex = csi;
-        SBSconfig.batchEndIndex = csi + currentBatch.length - 1;
+    SBSconfig.currentStepIndex = 0;
+    SBSconfig.isRunning = true;
+    SBSconfig.doneRunning = false;
+    SBSconfig.visibleItems = [];
+    SBSconfig.currentMaxNum = 0n;
+    SBSconfig.currentBatch = [];
+    SBSconfig.batchStartIndex = 0;
+    SBSconfig.batchEndIndex = 0;
 
-        sendSBS_Data(SBSconfig, currentBatch, SBSeventTarget, SBSconfig.batchStartIndex, SBSconfig.batchEndIndex);
+    runSBSTick();
+}
 
-        setTimeout(() => {
-            startSBS()
-            const ms = speedState.intervalMs;
-            const bs = speedState.batchSize;
-            console.log('ITS WORKIIIINGGGG');
-        }, ms);
+function runSBSTick() {
+    if (!SBSconfig.isRunning) return;
+
+    const workerResultLen = state.workerResult.length;
+    const batchSize = Math.max(1, Number(speedState.batchSize) || 1);
+    const startIndex = SBSconfig.currentStepIndex;
+
+    if (startIndex >= workerResultLen) {
+        finishSBS();
+        return;
     }
 
-    resumeSBS = startSBS;
+    currentBatch = state.workerResult.slice(startIndex, startIndex + batchSize);
+
+    if (currentBatch.length === 0) {
+        finishSBS();
+        return;
+    }
+
+    SBSconfig.visibleItems.push(...currentBatch);
+
+    for (let i = 0; i < currentBatch.length; i++) {
+        if (SBSconfig.currentMaxNum === 0n || currentBatch[i] > SBSconfig.currentMaxNum) {
+            SBSconfig.currentMaxNum = currentBatch[i];
+        }
+    }
+
+    const endIndex = startIndex + currentBatch.length - 1;
+    SBSconfig.currentStepIndex = startIndex + currentBatch.length;
+
+    sendSBS_Data(SBSconfig, currentBatch, stateTarget, startIndex, endIndex);
+
+    if (SBSconfig.currentStepIndex >= workerResultLen) {
+        finishSBS();
+        return;
+    }
+
+    const intervalMs = Math.max(0, Number(speedState.intervalMs) || 0);
+    timerId = setTimeout(() => {
+        timerId = null;
+        runSBSTick();
+    }, intervalMs);
+}
 
+export function SBSoutput() {
     stateTarget.addEventListener('collatz_done', () => {
         startSBS();
     });
-
-};
+}
diff --git a/ESmodules/ui/playButtonSVG.js b/ESmodules/ui/playButtonSVG.js
--- a/ESmodules/ui/playButtonSVG.js
+++ b/ESmodules/ui/playButtonSVG.js
@@
-import { SBSconfig } from "../state/state.js";
 import { runButton } from "./uiElements.js";
-import { pauseSBS } from "../core/SBSoutputManager.js";
 
-export function PlayOrPause() {
-    SBSconfig.isRunning = !SBSconfig.isRunning;
-
+export function setRunButtonMode(isPlaying) {
     if (!runButton) return;
 
-    if (SBSconfig.isRunning) {
+    if (isPlaying) {
         runButton.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="currentColor" class="ml-0.5"><path d="M6 4h4v16H6zM14 4h4v16h-4z"/></svg>`;
-        runButton.title = "РџР°СѓР·Р°";
-
-        pauseSBS();
-        console.log(`PlayOrPause: isRunning - ${SBSconfig.isRunning}`);
+        runButton.title = "Пауза";
     } else {
         runButton.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="currentColor" class="ml-0.5"><path d="M8 5v14l11-7z"/></svg>`;
-        runButton.title = "Р—Р°РїСѓСЃС‚РёС‚СЊ";
-
-        console.log(`PlayOrPause: isRunning - ${SBSconfig.isRunning}`);
+        runButton.title = "Запустить";
     }
 }
+
+export function PlayOrPause() {
+    setRunButtonMode(false);
+}
diff --git a/ESmodules/ui/runProcessControls.js b/ESmodules/ui/runProcessControls.js
--- a/ESmodules/ui/runProcessControls.js
+++ b/ESmodules/ui/runProcessControls.js
@@
 import { runProcess_Elements, mainInputField } from "./uiElements.js";
 import { getSpecificState, setStateValue } from "../state/stateManager.js";
 import { workerManager_Recieve } from "../workers/workerManager.js";
-import { PlayOrPause } from "./playButtonSVG.js";
+import { setRunButtonMode } from "./playButtonSVG.js";
+import { state, stateTarget, SBSconfig } from "../state/state.js";
+import { pauseSBS, resumeSBS } from "../core/SBSoutputManager.js";
+
+function resetRunData() {
+    state.workerResult = [];
+    state.workerListLen = 0;
+    state.workerMaxNum = 0n;
+    state.errorCause = null;
+
+    SBSconfig.currentStepIndex = 0;
+    SBSconfig.isRunning = false;
+    SBSconfig.doneRunning = false;
+    SBSconfig.visibleItems = [];
+    SBSconfig.currentMaxNum = 0n;
+    SBSconfig.currentBatch = [];
+    SBSconfig.batchStartIndex = 0;
+    SBSconfig.batchEndIndex = 0;
+}
 
 export function RunSequenceCalc() {
+    stateTarget.addEventListener('sbs_done', () => {
+        setRunButtonMode(false);
+    });
+
     runProcess_Elements.runButton.addEventListener('click', () => {
+        if (SBSconfig.isRunning) {
+            pauseSBS();
+            setRunButtonMode(false);
+            return;
+        }
+
+        const hasResult = state.workerResult.length > 0;
+        const canResume = hasResult &&
+            !SBSconfig.doneRunning &&
+            SBSconfig.currentStepIndex < state.workerResult.length;
+
+        if (canResume) {
+            resumeSBS();
+            setRunButtonMode(true);
+            return;
+        }
+
         // THE PRE-CHECK LAYER 
         if (mainInputField.value === '' || !/^\d+$/.test(mainInputField.value)) {
             alert('Error! Your input contains unacceptable symbols!');
             //  For the future -> ErrorWindowAppend();
             return; // exit the function if input is invalid
@@
         else {
+            resetRunData();
+
             // setting activeInputValue
             setStateValue('activeInputValue', BigInt(mainInputField.value));
     
             // sending the active input value to Worker Manager
             workerManager_Recieve(getSpecificState('activeInputValue'));
@@
-            // Play Pause change
-            PlayOrPause();
+            // Play Pause change
+            setRunButtonMode(true);
         }
 
     });
 }
diff --git a/ESmodules/visualisation/viewModes/sequenceView.js b/ESmodules/visualisation/viewModes/sequenceView.js
--- a/ESmodules/visualisation/viewModes/sequenceView.js
+++ b/ESmodules/visualisation/viewModes/sequenceView.js
@@
-import { stateTarget } from "../../state/state.js";
+import { stateTarget } from "../../state/state.js";
+import { seqList_Elements } from "../../ui/uiElements.js";
+import { txtListObj_create } from "./sequenceView_Logic.js";
 
-stateTarget.addEventListener('collatz_done', () => {
-    
+stateTarget.addEventListener('sbs_batch', (event) => {
+    const { batch, startIndex } = event.detail;
+
+    for (let i = 0; i < batch.length; i++) {
+        txtListObj_create(seqList_Elements.txtList, startIndex + i + 1, batch[i]);
+    }
 });
diff --git a/ESmodules/main.js b/ESmodules/main.js
--- a/ESmodules/main.js
+++ b/ESmodules/main.js
@@
 import { RunSequenceCalc } from './ui/runProcessControls.js';
 import '../webComponents/speedController.js';
+import './visualisation/viewModes/sequenceView.js';
 
 // input controls
 InputControlsHere(); // It should work..
